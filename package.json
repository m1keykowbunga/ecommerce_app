{
  "name": "don-palito-root",
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "web",
    "admin"
  ],
  "scripts": {
    "build": "npm install && npm run build -w web && npm run build -w admin",
    "start": "node backend/src/server.js"
  }
}
