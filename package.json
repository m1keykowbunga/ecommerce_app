{
  "name": "don-palito-root",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix web && npx --prefix web vite build && npm install --prefix admin && npx --prefix admin vite build",
    "start": "node backend/src/server.js"
  }
}