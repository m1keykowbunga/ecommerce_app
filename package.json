{
  "name": "don-palito-root",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix web && npm install --prefix admin",
    "build-web": "./web/node_modules/.bin/vite build --emptyOutDir --config ./web/vite.config.js",
    "build-admin": "./admin/node_modules/.bin/vite build --emptyOutDir --config ./admin/vite.config.js",
    "build-all": "npm run build-web && npm run build-admin",
    "build": "npm run install-all && npm run build-all",
    "start": "node backend/src/server.js"
  }
}
