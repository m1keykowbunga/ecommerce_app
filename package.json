{
  "name": "don-palito-root",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix web && cd web && npx vite build && cd .. && npm install --prefix admin && cd admin && npx vite build && cd ..",
    "start": "node backend/src/server.js"
  }
}