{
  "name": "don-palito-root",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix web && npm run build --prefix web && npm install --prefix admin && npm run build --prefix admin",
    "start": "node backend/src/server.js"
  }
}